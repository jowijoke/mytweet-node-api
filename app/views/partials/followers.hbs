<section class="ui raised segment">
    <div class="ui grid">
        <table class="ui celled table segment">
            <thead>
            <tr>
                <th>Followers</th>
            </tr>
            </thead>
            <tbody
            {{#each friendships}}
                <tr>

                        <td> <a href="/follower/{{_id}}">{{friends.id}} {{friends.lastName}}</a></td>
                        <td> <a href="/unfollow/{{_id}}"><button class="ui blue submit button">Unfollow</button></a> </td>

                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>
</section>